import { UndoRedoAction } from "./undo-redo";
import { Node, Group } from "../model/graph";
import { VisuallyJsRenderer } from "../renderer";
import { PointXY } from "../util";
/**
 * @internal
 */
export declare class MoveAction implements UndoRedoAction {
    obj: Node | Group;
    protected originalPosition: PointXY;
    pos: PointXY;
    protected renderer: VisuallyJsRenderer<any>;
    type: string;
    constructor(obj: Node | Group, originalPosition: PointXY, pos: PointXY, renderer: VisuallyJsRenderer<any>);
    redo(): void;
    undo(): void;
    hasDeltas(): boolean;
}
