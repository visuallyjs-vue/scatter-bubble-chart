import { OverlayPaintParams, SimpleShapeOverlay } from "./overlay";
import { UICore } from "../core";
import { Connection } from "../connector/connection-impl";
import { PaintStyle, SimpleShapeOverlayOptions } from "../../common";
import { PointXY } from "../../../core";
import { ConnectorPath } from "../connector/connectors";
/**
 * Default radius in X for a EllipseOverlay
 * @internal
 */
export declare const DEFAULT_ELLIPSE_OVERLAY_RADIUS_X = 8;
/**
 * Default radius in Y for a EllipseOverlay
 * @internal
 */
export declare const DEFAULT_ELLIPSE_OVERLAY_RADIUS_Y = 5;
/**
 * @internal
 */
export interface EllipseOverlayPaintParams extends OverlayPaintParams {
    atts: Array<{
        cx: number;
        cy: number;
        rx: number;
        ry: number;
        fill: string;
        stroke: string;
        "stroke-width": number;
    }>;
}
/**
 * Options for a Ellipse overlay
 * @group Edges
 * @category Overlays
 */
export interface EllipseOverlayOptions extends SimpleShapeOverlayOptions {
    /**
     * Radius in X. Defaults to 8
     */
    rx?: number;
    /**
     * Radius in Y. Defaults to 5
     */
    ry?: number;
}
/**
 * An ellipse overlay, with a configurable radius for x and for y
 * @group Edges
 * @category Overlays
 */
export declare class EllipseOverlay<EL> extends SimpleShapeOverlay<EL> {
    component: Connection<any>;
    rx: number;
    ry: number;
    constructor(instance: UICore<EL>, component: Connection<any>, params?: EllipseOverlayOptions);
    static type: string;
    type: string;
    contentTags: string[];
    draw(connector: ConnectorPath, currentConnectionPaintStyle: PaintStyle, absolutePosition?: PointXY): EllipseOverlayPaintParams;
}
