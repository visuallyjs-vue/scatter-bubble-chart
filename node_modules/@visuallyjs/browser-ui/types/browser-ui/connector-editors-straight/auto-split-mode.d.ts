import { StraightEditorHandleElement, StraightEditorMode, StraightSegmentEditorInfo } from "./definitions";
import { BrowserElement } from "../util";
import { StraightEditor } from "./straight-editor";
import { Connection } from "../../ui";
/**
 * Straight connector editor mode in which the user taps/presses the mouse at the
 * point they wish to split a segment, and a drag handle is drawn for them.
 * @internal
 */
export declare class AutoSplitMode implements StraightEditorMode {
    editor: StraightEditor;
    _edgeMousedown: (p: any) => void;
    _deleteListener: (e: {
        target: StraightEditorHandleElement;
    }) => void;
    private $segmentToDrag;
    private $pageEvent;
    constructor(editor: StraightEditor);
    $activate(conn: Connection<BrowserElement>, params?: any): void;
    $appendHandles(info: StraightSegmentEditorInfo): void;
    $deactivate(e?: Event): void;
    $repaintEnded(): void;
    $repaintStarted(): void;
}
