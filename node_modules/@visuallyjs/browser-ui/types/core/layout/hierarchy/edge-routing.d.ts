import { HierarchyLayoutModel } from "./model";
import { GatedPath, RouterOptions } from "../../router";
import { VertexSet } from "./vertex-set";
export declare class EdgeRoutingStage {
    private _model;
    private _vertexSet;
    private axis;
    private _invert;
    private _getRoutingInformation;
    private _vertexRegions;
    constructor(_model: HierarchyLayoutModel, _vertexSet: VertexSet, axis: number, _invert: boolean, _getRoutingInformation: () => RouterOptions);
    execute(): Record<string, GatedPath>;
    /**
     * Adds a gate, first computing the appropriate values
     * based upon the current axis.
     * @param entry
     * @param position
     * @param r1
     * @param r2
     * @param atEndInOtherAxis
     * @private
     */
    private _addGateForAxis;
    private _addGate;
}
