import { UICore, ViewportElement } from "../ui";
import { Size, AbstractLayoutAdapter } from "../core";
import { BrowserElement } from "./util";
/**
 * Implementation of layout adapter that is the parent of the `CanvasLayoutAdapter` and `GroupLayoutAdapter`, providing
 * the functionality shared by those two classes.
 * @internal
 */
export declare abstract class BaseDomUiLayoutAdapter extends AbstractLayoutAdapter<BrowserElement> {
    protected ui: UICore<BrowserElement>;
    protected constructor(ui: UICore<BrowserElement>);
    getViewportPositionById(id: string): ViewportElement<BrowserElement>;
    getSize(el: BrowserElement): Size;
}
