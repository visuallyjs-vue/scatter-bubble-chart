/**
 * Surface provider component. "Headless" in that it doesnt write out any elements of its own, it just writes out any child elements.
 */
declare const SurfaceProvider: {
    setup(): void;
    render(): any;
};
export { SurfaceProvider };
