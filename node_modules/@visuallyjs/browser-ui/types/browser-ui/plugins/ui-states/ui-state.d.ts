import { BrowserUI } from "../../browser-visuallyjs-instance";
import { TypeDescriptorBase, UICore } from "../../../ui";
import { DataSource, FilterableDataset } from "../../../core";
/**
 * @internal
 */
export declare class UIState<EL> {
    id: string;
    state: Record<string, TypeDescriptorBase>;
    ui: UICore<EL, any>;
    constructor(id: string, state: Record<string, TypeDescriptorBase>, ui: UICore<EL, any>);
    activate(target: FilterableDataset, renderer: BrowserUI, datasource: DataSource): void;
    deactivate(target: FilterableDataset, renderer: BrowserUI, dataSource: DataSource): void;
}
