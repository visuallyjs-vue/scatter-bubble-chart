import { Overlay } from "./overlay";
import { UICore } from "../core";
import { LabelOverlayOptions } from "../../common";
import { Connection } from "../connector/connection-impl";
import { ObjectData } from "../../../core";
import { FontSpec } from "../../../browser-ui";
/**
 * An overlay that draws some text. By default this overlay will use an HTML element, and that element's parent will be the surface canvas, unless the container is an SVG element, in which case the overlay will use an SVG element. You can set `useHTMLElement:false` in the options if you want the overlay to use an SVG element regardless of the container element type.
 * @group Edges
 * @category Overlays
 */
export declare class LabelOverlay<EL> extends Overlay<EL> {
    private instance;
    connection: Connection<EL>;
    label: string | Function;
    static type: string;
    type: string;
    useHTMLElement: boolean;
    rotatable: boolean | string;
    font: FontSpec;
    constructor(instance: UICore<EL>, connection: Connection<EL>, p: LabelOverlayOptions);
    isSimpleShape: boolean;
    getLabel(): string;
    setLabel(l: string | Function): void;
    updateFrom(d: any, context: ObjectData): void;
}
/**
 * @internal
 * @param o
 */
export declare function isLabelOverlay<EL>(o: Overlay<EL>): o is LabelOverlay<EL>;
/**
 * Updates the font for the given label overlay according to the values in data, which may be null
 * @param lo
 * @param data
 * @internal
 */
export declare function $updateLabelOverlayFont(lo: LabelOverlay<any>, data: ObjectData): void;
