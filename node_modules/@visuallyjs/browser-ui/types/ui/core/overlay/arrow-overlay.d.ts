import { SimpleShapeOverlay, SimpleShapeOverlayPaintParams } from "./overlay";
import { UICore } from "../core";
import { ArrowOverlayOptions, PaintStyle } from "../../common";
import { ConnectorPath } from "../connector/connectors";
import { Connection } from "../connector/connection-impl";
import { PointXY } from "../../../core";
/**
 * @internal
 */
export interface ArrowOverlayPaintParams extends SimpleShapeOverlayPaintParams {
    atts: Array<{
        d: string;
        fill: string;
        stroke: string;
        "stroke-width": number;
    }>;
}
/**
 * An arrow overlay, with a length, width, direction, location and foldback point.
 * @group Edges
 * @category Overlays
 */
export declare class ArrowOverlay<EL> extends SimpleShapeOverlay<EL> {
    component: Connection<any>;
    width: number;
    length: number;
    foldback: number;
    direction: number;
    static type: string;
    type: string;
    constructor(instance: UICore<EL>, component: Connection<any>, p: ArrowOverlayOptions);
    contentTags: string[];
    draw(connector: ConnectorPath, currentConnectionPaintStyle: PaintStyle, absolutePosition?: PointXY): ArrowOverlayPaintParams;
}
