import { RouterLine, RouterRegion, EdgeRouterOptions } from "./common";
import { PointXY } from "../util";
/**
 * Identifies the 'separate' orthogonal router mode.
 * @group Plugins
 * @category Edge Routing
 * @constant
 */
export declare const ORTHOGONAL_ROUTER_MODE_SEPARATE = "separate";
/**
 * Identifies the 'bus' orthogonal router mode.
 * @group Plugins
 * @category Edge Routing
 * @constant
 */
export declare const ORTHOGONAL_ROUTER_MODE_BUS = "bus";
/**
 * How to draw edges in 'orthogonal' mode in the {@link EdgeRoutingPlugin}. `bus` collates lines that are traversing a segment together into one line. `separate` draws lines that are traversing a segment together as parallel lines.
 * @group Plugins
 * @category Edge Routing
 */
export type OrthogonalRouterMode = typeof ORTHOGONAL_ROUTER_MODE_BUS | typeof ORTHOGONAL_ROUTER_MODE_SEPARATE;
/**
 * @internal
 */
export interface OrthogonalRouterOptions extends EdgeRouterOptions<OrthogonalRouterRegion> {
    axis: number;
    mode?: OrthogonalRouterMode;
    pad?: number;
}
/**
 * @internal
 */
export interface OrthogonalRouterRegion extends RouterRegion {
    lineSets: Array<Array<RouterLine>>;
}
/**
 * Route a set of orthogonal lines through a series of regions.
 * @param gates
 * @param edges
 * @param vertices
 * @param regions
 * @param mode
 * @internal
 */
export declare function orthogonalRouter(options: OrthogonalRouterOptions): Record<string, Array<PointXY>>;
