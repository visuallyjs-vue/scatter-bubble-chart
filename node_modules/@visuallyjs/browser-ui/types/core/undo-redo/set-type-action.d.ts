import { UndoRedoAction } from "./undo-redo";
import { Edge, Group, Node, Port } from "../model/graph";
import { VisuallyJsModel } from "../toolkit";
export declare class SetTypeAction implements UndoRedoAction {
    obj: Node | Port | Group | Edge;
    previousType: string;
    newType: string;
    protected instance: VisuallyJsModel;
    type: string;
    constructor(obj: Node | Port | Group | Edge, previousType: string, newType: string, instance: VisuallyJsModel);
    hasDeltas(): boolean;
    redo(): void;
    undo(): void;
}
