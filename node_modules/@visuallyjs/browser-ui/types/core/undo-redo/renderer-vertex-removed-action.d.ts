import { Group, Node } from "../model/graph";
import { VisuallyJsRenderer } from "../renderer";
import { UndoRedoAction } from "./undo-redo";
import { PointXY } from "../util";
/**
 * @internal
 */
export declare class VisuallyJsRendererVertexRemovedAction implements UndoRedoAction {
    private _renderer;
    vertex: Node | Group;
    pos: PointXY;
    type: string;
    constructor(_renderer: VisuallyJsRenderer<any>, vertex: Node | Group, pos: PointXY);
    redo(): void;
    undo(): void;
    hasDeltas(): boolean;
}
