import { Edge, Group, Node, Vertex } from "./graph";
export interface IGraph {
    nodes: Array<Node>;
    id: string;
    edges: Array<Edge>;
    groups: Array<Group>;
    getVertex(id: string): Vertex;
}
