import { Surface } from "../../surface";
import { PanZoom } from "../../../pan-zoom";
import { BaseBrowserUIPlugin, BrowserUIPlugin, BrowserUIPluginOptions } from "../../../plugins";
/**
 * Options for the pan buttons plugin.
 * @group Plugins
 * @category Pan Buttons
 */
export interface PanButtonsPluginOptions extends BrowserUIPluginOptions {
    /**
     * Time in milliseconds after the mouse button is pressed before panning should start. Defaults to 150.
     */
    startPanTimeout?: number;
    /**
     * Time in milliseconds between successive adjustment to the pan. Defaults to 60.
     */
    panRepeatInterval?: number;
    /**
     * Amount in pixels to adjust pan by each time the timer fires. Defaults to 10.
     */
    panRepeatDistance?: number;
    /**
     * Amount in pixels to pan the surface by when one of the buttons is pressed and released. Defaults to 50.
     */
    panDistance?: number;
}
/**
 * This is a Surface plugin that adds buttons around the perimeter of the surface canvas, which users can click on the nudge the pan in a certain direction. Click and hold auto-pans the surface canvas.
 * @group Plugins
 * @category Pan Buttons
 */
export declare class PanButtonsPlugin extends BaseBrowserUIPlugin implements BrowserUIPlugin<PanButtonsPluginOptions> {
    static type: string;
    id: "panButtons";
    startPanTimeout: number;
    panRepeatInterval: number;
    panRepeatDistance: number;
    _panButtons: Array<HTMLDivElement>;
    _startTimer: number;
    currentPanButton: any;
    surface: Surface;
    panZoom: PanZoom;
    panDistance: number;
    eventManager: any;
    private _justPanned;
    private _clearPanTimer;
    private _endPanRepeat;
    initialise(surface: Surface, options: PanButtonsPluginOptions): boolean;
    private curryPanButton;
    private startPanTimer;
    private makePanButton;
    destroy(): void;
    reset(): void;
}
