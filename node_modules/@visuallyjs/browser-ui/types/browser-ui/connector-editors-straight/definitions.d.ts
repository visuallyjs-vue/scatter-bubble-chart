import { Connection, StraightConnectorSegment } from "../../ui";
import { BrowserElement } from "../util";
import { BaseEdgeEditorHandleElement, ConnectorEditorOptions, SegmentEditorInfo } from "../connector-editors";
import { PointXY } from "../../core";
/** @internal */
export interface StraightEditorMode {
    $activate(conn: Connection<BrowserElement>, params?: any): void;
    $deactivate(e?: Event): void;
    $appendHandles(info: StraightSegmentEditorInfo): void;
    $repaintEnded(): void;
    $repaintStarted(): void;
}
/**
 * Added to a connector when the straight connector editor is active on it.
 * @group Edges
 * @category Path Editors
 * @constant
 */
export declare const CLASS_STRAIGHT_CONNECTOR_EDIT = "vjs-straight-connector-edit";
/**
 * Constant identifying the straight connector editor mode that shows a split button on each segment, which the user clicks to split the segment at its midpoint.
 * @group Edges
 * @category Path Editors
 * @constant
 */
export declare const STRAIGHT_CONNECTOR_CLICK_SPLIT = "click";
/**
 * Constant identifying the straight connector editor mode in which the user taps/presses the mouse at the point they wish to split a segment, and a drag handle is drawn for them.
 * @group Edges
 * @category Path Editors
 * @constant
 */
export declare const STRAIGHT_CONNECTOR_AUTO_SPLIT = "auto";
/**
 * Available modes for the Straight connector's split functionality.
 * @group Edges
 * @category Path Editors
 */
export type StraightConnectorEditorSplitMode = typeof STRAIGHT_CONNECTOR_CLICK_SPLIT | typeof STRAIGHT_CONNECTOR_AUTO_SPLIT;
/**
 * Options for a straight connector editor.
 * @group Edges
 * @category Path Editors
 */
export interface StraightConnectorEditorOptions extends ConnectorEditorOptions {
    /**
     * Identifies the mode to use to split segments. {@link STRAIGHT_CONNECTOR_AUTO_SPLIT} creates a new segment handle when the user performs a mousedown or touchstart, at the location of the event. This is the default. {@link STRAIGHT_CONNECTOR_CLICK_SPLIT} displays a button on the midpoint of each segment which the user can click on to split the segment.
     */
    splitMode?: StraightConnectorEditorSplitMode;
    /**
     * Defaults to true - when the connector is smoothed, show guidelines linking each of the control points in the path.
     */
    showGuidelines?: boolean;
}
/** @internal */
export interface StraightSegmentEditorInfo extends SegmentEditorInfo<StraightEditorHandleElement, StraightConnectorSegment> {
    focus: StraightConnectorSegment;
    previous: StraightConnectorSegment;
    index: number;
    buttons: Array<StraightEditorHandleElement>;
    originalLocation: PointXY;
    midPoint: PointXY;
}
/**
 * @internal
 */
export interface StraightEditorHandleElement extends BaseEdgeEditorHandleElement<StraightSegmentEditorInfo> {
    _vjsDragHandle: StraightSegmentEditorInfo;
}
