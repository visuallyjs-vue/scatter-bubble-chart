import { StraightEditor } from "./straight-editor";
import { StraightEditorMode, StraightSegmentEditorInfo } from "./definitions";
import { Connection } from "../../ui";
import { BrowserElement } from "../util";
/**
 * The operating mode in which a split button is added to the center of each segment, and the user clicks this button to split the segment, which
 * then converts into a drag handle.
 * @internal
 */
export declare class ClickSplitMode implements StraightEditorMode {
    editor: StraightEditor;
    private splitListener;
    private deleteListener;
    constructor(editor: StraightEditor);
    $activate(conn: Connection<BrowserElement>, params?: any): void;
    $deactivate(e?: Event): void;
    $appendHandles(info: StraightSegmentEditorInfo): void;
    $repaintEnded(): void;
    $repaintStarted(): void;
}
