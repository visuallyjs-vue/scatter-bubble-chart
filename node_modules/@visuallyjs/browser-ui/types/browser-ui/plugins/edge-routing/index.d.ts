import { RelayoutParams } from "../../../ui";
import { VisuallyJsModel, HierarchyLayout, EdgeRouterOptions, OrthogonalRouterMode } from "../../../core";
import { BaseBrowserUIPlugin, BrowserUIPlugin, BrowserUIPluginOptions } from "../browser-ui-plugin";
import { BrowserUI } from "../../browser-visuallyjs-instance";
/**
 * Identifies the direct line routing mode.
 * @group Plugins
 * @category Edge Routing
 */
export declare const EDGE_ROUTING_PLUGIN_MODE_DIRECT = "direct";
/**
 * Identifies the orthogonal line routing mode.
 * @group Plugins
 * @category Edge Routing
 */
export declare const EDGE_ROUTING_PLUGIN_MODE_ORTHOGONAL = "orthogonal";
/**
 * Available modes for the edge routing plugin.
 * @group Plugins
 * @category Edge Routing
 */
export type EdgeRoutingPluginMode = typeof EDGE_ROUTING_PLUGIN_MODE_DIRECT | typeof EDGE_ROUTING_PLUGIN_MODE_ORTHOGONAL;
/**
 * Options for the edge routing plugin.
 * @group Plugins
 * @category Edge Routing
 */
export interface EdgeRoutingPluginOptions extends BrowserUIPluginOptions {
    /**
     * Mode to use to draw edges. 'orthogonal' is like the orthogonal connector - segments that are either vertical or horizontal.
     * `direct` draws straight lines through regions. Both modes assign a separate anchor point to each edge.
     */
    mode: EdgeRoutingPluginMode;
    /**
     * When mode is 'orthogonal' this instructs VisuallyJs whether to group edges in buses, or to route each edge separately.
     */
    orthogonalMode?: OrthogonalRouterMode;
    /**
     * How much space to leave between stacked orthogonal edges. Defaults to 10 pixels.
     */
    orthogonalPadding?: number;
}
/**
 * A plugin that can ingest the RoutingInformation created by a HierarchyLayout, and then route edges according to the data.
 * You must be using a `Straight` connector if you use this plugin (which is the default connector).
 *
 * @group Plugins
 * @category Edge Routing
 */
export declare class EdgeRoutingPlugin extends BaseBrowserUIPlugin implements BrowserUIPlugin<EdgeRoutingPluginOptions> {
    static type: string;
    id: "edgeRouting";
    ui: BrowserUI;
    instance: VisuallyJsModel;
    mode: 'direct' | 'orthogonal';
    orthogonalMode: OrthogonalRouterMode;
    orthogonalPadding: number;
    layoutListener: (rp: RelayoutParams<HierarchyLayout>, e: Event) => void;
    _routingInformation: Array<EdgeRouterOptions>;
    private _positions;
    private _sizes;
    destroy(): void;
    private _route;
    initialise(ui: BrowserUI, options: EdgeRoutingPluginOptions): boolean;
    reset(): void;
}
