import { UndoRedoAction } from "./undo-redo";
import { Group, Node } from "../model/graph";
import { VisuallyJsModel } from "../toolkit";
import { VisuallyJsRenderer } from "../renderer";
import { PointXY } from "../util";
/**
 * @internal
 */
declare abstract class GroupAction {
    protected node: Node;
    protected group: Group;
    protected instance: VisuallyJsModel;
    protected constructor(node: Node, group: Group, instance: VisuallyJsModel);
    hasDeltas(): boolean;
}
export declare class AddGroupMemberAction extends GroupAction implements UndoRedoAction {
    protected node: Node;
    protected group: Group;
    protected sourceGroup: Group;
    private _originalMemberPosition;
    private _memberPositionInGroup;
    protected instance: VisuallyJsModel;
    protected renderer: VisuallyJsRenderer<any>;
    type: string;
    constructor(node: Node, group: Group, sourceGroup: Group, _originalMemberPosition: PointXY, _memberPositionInGroup: PointXY, instance: VisuallyJsModel, renderer: VisuallyJsRenderer<any>);
    redo(): void;
    undo(): void;
}
export declare class RemoveGroupMemberAction extends GroupAction implements UndoRedoAction {
    protected node: Node;
    protected group: Group;
    protected targetGroup: Group;
    private _originalPosition;
    private _newPosition;
    protected instance: VisuallyJsModel;
    protected renderer: VisuallyJsRenderer<any>;
    type: string;
    constructor(node: Node, group: Group, targetGroup: Group, _originalPosition: PointXY, _newPosition: PointXY, instance: VisuallyJsModel, renderer: VisuallyJsRenderer<any>);
    redo(): void;
    undo(): void;
}
export declare class GroupCollapseAction implements UndoRedoAction {
    group: Group;
    renderer: VisuallyJsRenderer<any>;
    type: string;
    constructor(group: Group, renderer: VisuallyJsRenderer<any>);
    redo(): void;
    undo(): void;
    hasDeltas(): boolean;
}
export declare class GroupExpandAction implements UndoRedoAction {
    group: Group;
    renderer: VisuallyJsRenderer<any>;
    type: string;
    constructor(group: Group, renderer: VisuallyJsRenderer<any>);
    redo(): void;
    undo(): void;
    hasDeltas(): boolean;
}
export {};
