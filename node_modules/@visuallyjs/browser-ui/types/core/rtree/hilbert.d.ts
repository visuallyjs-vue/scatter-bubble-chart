import { RectangleXY } from "../util";
/**
 * @internal
 */
export declare const HilbertCurves: {
    sortRect: <T extends RectangleXY>(listOfRectangles: Array<T>) => Array<T>;
    toHilbertCoordinates: (maxCoordinate: number, x: number, y: number) => number;
    hilbertInterleaveBits: (odd: number, even: number) => number;
};
