import { EditorBase } from "../connector-editors";
import { Group, Node, PointXY } from "../../core";
import { InternalStraightConnectorGeometry, StraightConnector, StraightConnectorSegment, Connection } from "../../ui";
import { BrowserUI } from "../browser-visuallyjs-instance";
import { BrowserElement } from "../util";
import { StraightConnectorEditorOptions, StraightSegmentEditorInfo } from "./definitions";
/**
 * Path editor for Straight connectors.
 * @group Edges
 * @category Path Editors
 */
export declare class StraightEditor extends EditorBase<StraightConnector> {
    private $mode;
    private $showGuidelines;
    static type: string;
    geometry: InternalStraightConnectorGeometry;
    segments: Array<StraightConnectorSegment>;
    segmentHandles: Array<StraightSegmentEditorInfo>;
    guideLine: SVGElement;
    $supportsHoverMode: boolean;
    $split(segmentIndex: number, atPoint: PointXY): void;
    $deleteSegment(segmentInfo: StraightSegmentEditorInfo): void;
    constructor(ui: BrowserUI, params: StraightConnectorEditorOptions);
    /**
     * Called by the superclass upon activation.
     * @param conn
     * @param params
     * @private
     */
    $activate(conn: Connection<BrowserElement>, params?: any): void;
    /**
     * Removed all the handles, and the guideline, if drawn.
     * @param excludeIndex
     * @private
     */
    $clearHandles(excludeIndex?: number): void;
    $elementDragged(vertex: Node | Group): void;
    $elementDragging(vertex: Node | Group): void;
    $repaint(args?: any): void;
    private _maybeDrawGuideline;
    deactivate(e?: Event): void;
}
