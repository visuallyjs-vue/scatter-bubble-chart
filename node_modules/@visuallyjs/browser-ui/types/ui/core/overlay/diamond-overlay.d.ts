import { ArrowOverlay } from "./arrow-overlay";
import { ArrowOverlayOptions } from "../../common";
import { Connection } from "../connector/connection-impl";
import { UICore } from "../core";
/**
 * Draws a diamond overlay. Internally this is an arrow overlay with a foldback point set to 2.
 * @group Edges
 * @category Overlays
 */
export declare class DiamondOverlay<EL> extends ArrowOverlay<EL> {
    static type: string;
    type: string;
    constructor(instance: UICore<EL>, component: Connection<any>, p: ArrowOverlayOptions);
}
