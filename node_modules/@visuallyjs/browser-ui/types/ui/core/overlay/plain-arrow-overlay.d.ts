import { ArrowOverlay } from "./arrow-overlay";
import { UICore } from "../core";
import { ArrowOverlayOptions } from "../../common";
import { Connection } from "../connector/connection-impl";
/**
 * A PlainArrow is an arrow with a flat baseline, ie. it does not fold back at all. Internally
 * this overlay is simply an `Arrow` overlay with `foldback` set to 1.
 * @group Edges
 * @category Overlays
 */
export declare class PlainArrowOverlay<EL> extends ArrowOverlay<EL> {
    static type: string;
    type: string;
    constructor(instance: UICore<EL>, component: Connection<any>, p: ArrowOverlayOptions);
}
