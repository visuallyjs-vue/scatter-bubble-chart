import { EdgeRouterOptions } from "./common";
import { PointXY } from "../util";
/**
 * @internal
 */
export interface DirectRouterOptions extends EdgeRouterOptions {
}
/**
 * @internal
 * @param options
 */
export declare function directRouter(options: DirectRouterOptions): Record<string, Array<PointXY>>;
