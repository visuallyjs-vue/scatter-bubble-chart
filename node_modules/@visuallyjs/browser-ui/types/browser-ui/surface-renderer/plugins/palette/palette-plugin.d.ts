import { BaseBrowserUIPlugin, BrowserUIPlugin } from "../../../plugins";
import { PaletteOptions } from "../../../components";
import { BrowserElement } from "../../../util";
import { BrowserUI } from "../../../browser-visuallyjs-instance";
/**
 * Options for the palette plugin
 * @group Plugins
 * @category Palette
 */
export interface PalettePluginOptions extends PaletteOptions<BrowserElement> {
}
export declare class PalettePlugin extends BaseBrowserUIPlugin implements BrowserUIPlugin<PalettePluginOptions> {
    static type: string;
    id: "palette";
    private $palette;
    destroy(): void;
    initialise(ui: BrowserUI, options: PalettePluginOptions): boolean;
    reset(): void;
}
