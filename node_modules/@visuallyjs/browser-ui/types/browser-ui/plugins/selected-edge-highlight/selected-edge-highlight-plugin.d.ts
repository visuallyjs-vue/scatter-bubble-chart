import { BaseBrowserUIPlugin, BrowserUIPlugin, BrowserUIPluginOptions } from "../browser-ui-plugin";
import { BrowserUI } from "../../browser-visuallyjs-instance";
import { Edge, VertexUpdatedReason } from "../../../core";
import { PaintStyle, ViewportElement } from "../../../ui";
import { BrowserElement } from "../../util";
export interface SelectedEdgeHighlightPluginOptions extends BrowserUIPluginOptions {
    className?: string;
    paintStyle?: PaintStyle;
}
export declare class SelectedEdgeHighlightPlugin extends BaseBrowserUIPlugin implements BrowserUIPlugin<SelectedEdgeHighlightPluginOptions> {
    static type: string;
    id: string;
    private _highlights;
    private _className;
    private _style;
    private _ui;
    initialise(ui: BrowserUI, options: SelectedEdgeHighlightPluginOptions): boolean;
    destroy(): void;
    reset(): void;
    private _handleSelect;
    private _addUpdateListeners;
    private _cleanupUpdateListeners;
    $edgeRemoved(edge: Edge): void;
    $edgeUpdated(edge: Edge): void;
    $vertexUpdated(element: ViewportElement<BrowserElement>, reason: VertexUpdatedReason): void;
    private _handleDeselect;
    private _handleSelectionCleared;
    private _handleEdgeGeometryUpdated;
    private _handleEdgePathEdited;
    private __handleVertex;
    private _handleVertexMove;
    private _drawHighlight;
    private _updateHighlight;
    private _removeHighlight;
    private _applyBounds;
}
