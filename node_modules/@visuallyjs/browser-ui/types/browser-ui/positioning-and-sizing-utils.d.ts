import { BrowserElement } from "./util";
import { PointXY, Size, RectangleXY } from "../core";
import { ViewportElement } from "../ui";
/**
 * @internal
 */
export declare const ATTRIBUTE_TRANSFORM = "transform";
/**
 * Gets the size of the element, which is calculated in various different ways depending on what sort of element is
 * passed in. This method is the one we use most often.
 * padding et
 * @param el
 * @internal
 */
export declare function getElementSize(el: Element): Size;
export declare function getPosition(el: BrowserElement, knownElementSize?: Size): PointXY;
export declare function setPosition(el: BrowserElement, p: PointXY): void;
/**
 * Gets the position of an element relative to the some other SVG,
 * even with nested SVG elements and multiple transforms/rotations.
 * @internal
 */
export declare function getSvgPositionRelativeToSvgRoot(el: SVGGraphicsElement, rootSvg: SVGSVGElement, useBBox?: boolean): PointXY;
/**
 * Sets the position of the given element in the X axis.
 * @param el
 * @param x
 * @internal
 */
export declare function setXPosition(el: BrowserElement, x: number): void;
/**
 * Sets the position of the given element in the Y axis.
 * @param el
 * @param y
 * @internal
 */
export declare function setYPosition(el: BrowserElement, y: number): void;
/**
 * Sets the size of the element, which is executed in various different ways depending on what sort of element is
 * passed in.
 * @param el
 * @param w
 * @param h
 * @internal
 */
export declare function setElementSize(el: BrowserElement, w: number, h: number): void;
/**
 * Sets the width of the element, which is executed in various different ways depending on what sort of element is
 * passed in.
 * @param el
 * @param w
 * @internal
 */
export declare function setElementWidth(el: BrowserElement, w: number): void;
/**
 * Sets the height of the element, which is executed in various different ways depending on what sort of element is
 * passed in.
 * @param el
 * @param h
 * @internal
 */
export declare function setElementHeight(el: BrowserElement, h: number): void;
/**
 * Gets the position of the given element as calculated by reading its `left` and `top` from its style.
 * @param el
 * @internal
 */
export declare function getAbsolutePosition(el: any): PointXY;
/**
 * Sets the position of the given element by writing values to its `left` and `top`
 * style properties. This can be used if the parent element is definitely an HTML
 * element, such as for the palette, because its elements are added to the document body.
 * @param el
 * @param p
 * @internal
 */
export declare function setAbsolutePosition(el: BrowserElement, p: PointXY): void;
/**
 * Returns the position of the given element as calculated by reading its `translate` attribute (or style property?).
 * @internal
 * @param el
 */
export declare function getSvgTranslatePosition(el: SVGElement, knownElementSize?: Size): PointXY;
/**
 * Sets the rotation of the given element. If an HTML element, or an SVG element whose parent is an HTML element, this method sets the `rotation` of the element's transform, inside its style object, and ensures the transform origin is set to the element's center. If the element's parent is an SVG element, we instead set a `rotate` value in the element's transform attribute, retaining any `translate` value that the transform may have. Elements whose parent is an SVG element their `translate` value as the transform origin. By default this is 0,0, meaning elements rotate around the SVG origin. But if you want to spin an element around its own center, you first have to translate the element. This method does NOT do that for you. The translation required is not entirely intuitive: the element gets rotated by its top left corner, so you have to move it to a place where rotating by its top left corner results in it appearing where you want it. In practice this point is computed by taking the element's top left corner and rotating it around the element's center. But like I said, that occurs upstream of this method.
 *
 * An example of this method being called on an svg element is this:
 *
 *  - first rotate the element's by its around its center
 * const translatedOrigin = rotatePoint(mel, mel.c, mel.r)
 * - maybe adjust for group
 *  adjustForGroup(translatedOrigin)
 *  - set this rotated pos as the origin
 * setSvgTranslatePosition(_el as any, translatedOrigin)
 * - now apply rotation.
 * setElementRotation(_el, mel.r)
 *
 * @internal
 * @param el
 * @param rotation
 */
export declare function setElementRotation(el: BrowserElement, rotation: number): void;
/**
 * Sets the position of some SVG element by setting a `translate` in the transform of the element. This method retains
 * any `rotation` that was already set in the element's transform.
 * @internal
 * @param el
 * @param p
 */
export declare function setSvgTranslatePosition(el: SVGElement, p: PointXY): void;
/**
 * Computes the translation required for the given box so that when it is rotated it appears to have been rotated around its center. This is required when working with SVG elements (whose parent is an SVG element; it doesnt apply when you have some SVG element whose parent is an HTML element, because its a function of how the parent behaves).
 *
 * The reason this is required is that SVG elements have their rotation applied using the parent SVG's top left corner as the transform origin. HTML elements can have the transform origin set, via css or the style.
 *
 *
 * @internal
 * @param box
 * @param p
 * @param r
 */
export declare function computeTranslationForRotation(box: RectangleXY, p: PointXY, r: number): import("../ui").RotatedPointXY;
export declare function setRotatedSvgElementPosition(el: ViewportElement<BrowserElement>, p: PointXY): void;
